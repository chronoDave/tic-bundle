{
  "name": "tic-bundle",
  "version": "2.1.1",
  "description": "Multi-file development in TIC-80",
  "main": "src/index.js",
  "keywords": [
    "tic80",
    "tic-80",
    "fantasy-console",
    "javascript",
    "babel"
  ],
  "repository": "https://github.com/chronoDave/tic-bundle.git",
  "author": "Chronocide",
  "license": "GPLv3",
  "bin": "./index.js",
  "scripts": {
    "test": "tape src/index.test.js",
    "bump:patch": "yarn version --patch && git push --tags && yarn publish",
    "bump:minor": "yarn version --minor && git push --tags && yarn publish",
    "bump:major": "yarn version --major && git push --tags && yarn publish"
  },
  "dependencies": {
    "@babel/standalone": "^7.9.6",
    "babel-preset-env": "^1.7.0",
    "chokidar": "^3.4.0",
    "deepmerge": "^4.2.2",
    "fast-glob": "^3.2.4",
    "minimist": "^1.2.5"
  },
  "devDependencies": {
    "eslint": "^7.0.0",
    "eslint-config-airbnb-base": "^14.1.0",
    "eslint-plugin-import": "^2.22.0",
    "tape": "^5.0.1"
  }
}
